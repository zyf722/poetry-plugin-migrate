[tool.poetry]
name = "local-absolute-package"
version = "0.1.0"
description = ""
authors = ["MaxMixAlex <MaxMixAlex@protonmail.com>"]

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
